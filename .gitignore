# System
.DS_Store

# Sensitive / runtime
container.json
.env
*.pem

# Large binary sources (keep text, ignore large downloads)
sources/*.pdf
sources/*.html
sources/*.mp4
sources/*.mp3
venv/

# Backtesting cache (large binary parquet + text caches)
backtesting/cache/

# Python compiled
backtesting/daily/__pycache__/

# Podcast audio segments (large binaries)
podcasts/audio/*.mp3
*.mp3
