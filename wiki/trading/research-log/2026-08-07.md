# Research Log — 2026-08-07

## Note: this cycle's work was already completed earlier tonight

This "Nightly trading research session" task fires at 2:30 AM CT, but a prior
task run already completed both phases for tonight's cycle at ~23:15-23:18 PM
CT on 2026-08-06 (commits `6e1fe98`, `122edf4`, `398564e`) — staged under
`dream_cycle/staged/2026-08-07/` per the standing "stage under tomorrow's date
so the 4 AM build phase finds it" convention. Only the research-log write step
(this file) was missing, so this entry closes that gap rather than re-running
the scan. Re-running would duplicate arXiv/GitHub search tokens and risk
conflicting staged proposal files per the standing duplicate-scan gotcha.

## Wiki Expansion (completed 2026-08-06 23:15 CT)

**Target:** `data-sources/` — new page `point-in-time-constituents.md`,
consolidating point-in-time index membership and vintage-fundamentals sourcing
options (fja05680/sp500, Wikipedia revision API, SEC EDGAR XBRL `filed` dedup,
SEC EDGAR SIC codes, Norgate, Sharadar, WRDS CRSP, Bloomberg). This closes a
gap that H158, H167, and H277 each independently hit and treated as a one-off
blocker rather than a documented reference: H158 DEFERRED and H167 BLOCKED
pending PIT data, H277 CONFIRMED but flagged NOT FOR PRODUCTION due to survivorship
bias in its hand-picked NASDAQ universe. Includes a decision framework for when
free sources (large-cap, low-turnover) are sufficient vs. when a paid vendor
(small/mid-cap, ML multi-factor) is actually justified, and cross-links to the
existing `survivorship-bias.md` and `sector-classification.md` pages. `wiki/index.md`
updated with the new entry.

## Dream Cycle Scan (completed 2026-08-06 23:15-23:18 CT)

Three high-relevance arXiv findings staged for the 4 AM build phase, all NOT
YET APPLIED (`apply_status: pending`):

1. **FinCAD** (arXiv:2605.24564, Li/Wang/Ma, May 2026) — inference-time
   Context-Aware Decoding fix for *parametric* look-ahead bias (an LLM's
   pretraining recall of a historical outcome, not a data-pipeline leak).
   Cuts in-sample returns on memorized dates by up to -67.1% while leaving OOS
   Sharpe within ~0.10 of baseline. Caveat: needs decoding-time logit access —
   not directly usable against George's frontier closed-model API calls
   (GPT-4o-class, Claude) without a black-box workaround. Staged to append to
   `backtesting/lookahead-formal-verification.md`.
2. **HindsightBench** (arXiv:2607.18867, Jia, Jul 2026) — the black-box
   companion to FinCAD: a four-arm date-manipulation audit protocol (revealed/
   date-only/masked/transplanted) that detects parametric hindsight using only
   API access, no logprobs — matching George's actual access level. ~$19-30 per
   audit row. Staged to append to `algorithms/llm-alpha-validation.md` as a
   concrete pre-flight gate for H185/H381-H384/H408-style LLM-in-the-loop work.
3. **Hyperbolic alpha decay from factor crowding** (arXiv:2512.11913) — offers
   a candidate *mechanism* (crowding-equilibrium-derived hyperbolic decay,
   momentum-specific, accelerating post-2015) for the H198/H435-H437 momentum
   degradation already observed but previously unexplained. **Flagged WITHDRAWN
   by dreamcycle-2026-05-29 peer review before this entry was finalized** — the
   paper is pending major revisions on arXiv as of 2025-12-27. Staged content
   was rewritten to frame it explicitly as an unvalidated hypothesis, not a
   confirmed result, before appending to `backtesting/signal-halflife.md`.

Scan summary and per-paper relevance scoring: see prior night's equivalent at
`dream_cycle/research/2026-08-06_scan.json` for format reference; no new scan
JSON produced tonight since no new scanning was performed (see note above).

## Status

All three proposals remain `apply_status: pending`, awaiting the 4 AM build
phase on 2026-08-07 as originally scheduled. No further action taken this
session — see note at top.
