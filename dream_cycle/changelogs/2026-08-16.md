# Dream Cycle Changelog — 2026-08-16

**Applied:** 0
**Skipped:** 0
**Flagged (high-risk, needs Kevin review):** 1

## Flagged for review

### Retrofit filelock-based mutex into PEAD/PEAD-GAP execution scripts + dream-cycle build phase

- **File:** `dream_cycle/staged/2026-08-16/1_filelock_retrofit_pead_execution_scripts.json`
- **Risk:** high (proposes changes to live order-submission scripts)
- **Target scripts:** `pead_gap_open.py`, `pead_open.py`, `pead_exits.py`, `pead_gap_exits.py`, plus the dream-cycle build-phase apply step
- **Source:** Reflection on tonight's Phase 1 wiki research (`wiki/trading/paper-trading/idempotency-concurrency-control.md`), grounded in the documented incident history in `task-registry.md`:
  - PEAD-GAP open-pass: 4 invocations within 65 seconds on 2026-07-31; only avoided a duplicate order because of an incidental "already in positions" check, not a designed guard.
  - Dream-cycle build phase: double wiki-append on 2026-08-01 after a compaction-resumed session re-entered the apply loop.
  - Two separate git add/commit sweep incidents on 2026-08-03 where concurrent sessions' commits picked up each other's staged files.
- **Proposed fix:** wrap each script's run body in a `filelock.FileLock` (PyPI, 975★ GitHub, MIT, actively maintained) with a short timeout; on lock contention, log and exit quietly instead of proceeding — treats a duplicate trigger as a safe no-op rather than relying on incidental guards. Full design + code example in the wiki page (Pattern 1).
- **Why flagged instead of auto-applied:** touches scripts that submit real (paper) orders and the build phase that mutates wiki/hypothesis-log content — build policy requires high-risk items skip auto-apply and go to Kevin for review. `filelock` is also not yet installed in `venv/`; installing it requires either an interactive session or explicit approval per the off-hours package-install policy, which this 4 AM build phase is not authorized to grant itself.
- **Next step if approved:** install `filelock` in venv, apply the wrapper to the 4 execution scripts + build-phase apply loop, verify with a dry run before the next scheduled trigger.
